```mermaid
architecture-beta
    group aws(cloud)[AWS Cloud]
    
    service user(internet)[End User]
    
    group edge(aws)[Edge & Security]
    service waf(waf)[AWS WAF] in edge
    service cdn(cloudfront)[CloudFront] in edge
    
    group region(aws)[Region: us-east-1]
    
    service api(api-gateway)[API Gateway] in region
    
    group vpc(aws)[VPC - No NAT] in region
    
    group public(aws)[Public Subnet] in vpc
    
    group private(aws)[Private Subnets] in vpc
    service lambda(lambda)[Backend Lambda] in private
    service vpce_bedrock(vpc)[Interface Endpoint\nBedrock] in private
    service vpce_textract(vpc)[Interface Endpoint\nTextract] in private
    
    group data(aws)[Data & Storage] in region
    service s3_static(s3)[S3 Frontend] in data
    service s3_uploads(s3)[S3 Uploads] in data
    service ddb(dynamodb)[DynamoDB] in data
    
    group ai(aws)[AI & ML] in region
    service bedrock(bedrock)[Amazon Bedrock] in ai
    service textract(textract)[AWS Textract] in ai
    
    group ops(aws)[Ops & Security] in region
    service cw(cloudwatch)[CloudWatch] in ops
    service sns(sns)[SNS Alerts] in ops
    service kms(kms)[KMS CMK] in ops
    
    user:R --> L:cdn
    cdn:R --> L:waf
    cdn:B --> T:api
    cdn:T --> B:s3_static
    
    api:R --> L:lambda
    
    lambda:T --> B:vpce_bedrock
    vpce_bedrock:T --> B:bedrock
    
    lambda:R --> L:vpce_textract
    vpce_textract:R --> L:textract
    
    lambda:B --> T:ddb
    lambda:B --> T:s3_uploads
    
    s3_uploads:R --> L:kms
```
